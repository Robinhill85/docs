---
title: Forked subagents
description: Inherit the parent's conversation and system prompt instead of starting a subagent cold
tag: "Beta"
---

import ForkedSubagentsBasicPy from '/snippets/code-samples/forked-subagents-basic-py.mdx';
import ForkedSubagentsBasicJs from '/snippets/code-samples/forked-subagents-basic-js.mdx';
import ForkedSubagentsCompiledPy from '/snippets/code-samples/forked-subagents-compiled-py.mdx';
import ForkedSubagentsCompiledJs from '/snippets/code-samples/forked-subagents-compiled-js.mdx';

A **forked subagent** inherits the parent's full conversation history and exact system prompt. In contrast, other [subagents](/oss/deepagents/subagents) receive only a task description and have no memory of the conversation that led up to the delegation.

Use forking when you delegate deep into an investigation to avoid subagents re-deriving context the parent already gathered. For example, hand an in-progress incident investigation to a subagent that drafts the postmortem.

<div className="skills-composition-diagram">

![A forked subagent replays the parent's prior exploration steps, then continues working on its own before handing an answer back. It never starts from a blank slate.](/oss/images/deepagents/forked-subagents-diagram.svg)

</div>

<Warning>
    Subagent forking is in [**beta**](/oss/versioning). APIs and behavior may change between releases.
</Warning>

## Configure a forked subagent

:::python
Set `mode: "fork"` on a @[`ForkedSubAgent`]. The spec shares most @[`SubAgent`] fields:
`name`, `description`, `tools`, `model`, `middleware`, `interrupt_on`, `permissions`, and `response_format`.

`system_prompt` and `skills` are rejected if provided. A forked subagent always uses the parent's inherited system prompt, so a fork-specific prompt or skills injection would have no effect.

<ForkedSubagentsBasicPy />
:::

:::js
Set `mode: "fork"` on a @[`ForkedSubAgent`]. The spec shares most @[`SubAgent`] fields:
`name`, `description`, `tools`, `model`, `middleware`, `interruptOn`, `permissions`, and `responseFormat`.

`systemPrompt` and `skills` are rejected if provided. A forked subagent always uses the parent's inherited system prompt, so a fork-specific prompt or skills injection would have no effect.

<ForkedSubagentsBasicJs />
:::

## How it works

A forked subagent's system message is not a static copy of the parent's prompt. Deep Agents captures whatever the parent's middleware produced on its last call (skills injection, memory, custom prompt mutation) and replays that message into the forked subagent.

That shared prefix lets the fork reuse the parent's prompt cache instead of starting cold. The fork's own tools still work normally, but expect cache misses where the fork's tool use diverges from the parent's.

The inherited history also includes a short preamble that marks it as a continuation, not a fresh request.

## Fork a `CompiledSubAgent`

A [`CompiledSubAgent`](/oss/deepagents/subagents#compiledsubagent) can also set `mode: "fork"`. This inherits the parent's message history the same way as a @[`ForkedSubAgent`], but keeps the compiled graph's own system prompt. A `CompiledSubAgent` is already fully built, so it cannot inherit a system message.

:::python
<ForkedSubagentsCompiledPy />
:::

:::js
<ForkedSubagentsCompiledJs />
:::

## When to use forking

To consider when to use forking, consider the following dimensions:

| Dimension | Isolated (default) | Forked |
|-----------|---------------------|--------|
| **Context** | Only the task description you pass in | The parent's full conversation history and system prompt |
| **Prompt caching** | Builds a new prompt from scratch | Reuses the parent's cached prompt prefix |
| **System prompt and skills** | You set them on the subagent | Not settable; the fork uses the parent's |
| **Calling other subagents** | Can use the `task` tool | Cannot use `task`; must finish the work itself |
| **Best for** | Focused work that needs little prior context | Continuing an investigation the parent has already started |

## See also

- [Subagents](/oss/deepagents/subagents): Configure subagent names, descriptions, and system prompts
- [Dynamic subagents](/oss/deepagents/dynamic-subagents): Dispatch subagents from interpreter code
- [Context engineering](/oss/deepagents/context-engineering): How conversation history is summarized as it grows
